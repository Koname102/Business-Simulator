=== COMPANY CREATION PAGE ===

FILE: app/onboarding/company/page.tsx
URL: /onboarding/company

=== FUNGSI ===
Input nama perusahaan dan pilih tipe bisnis sebelum mulai game

=== DATA SOURCE ===
Load dari localStorage key 'temp_player' (dari character creation)

=== STATE ===
- playerData: { name, age } dari localStorage
- companyName: string (input company name)
- businessType: 'fintech' | 'insurance' | 'investment' (default fintech)
- error: string (pesan error validasi)

=== BUSINESS OPTIONS ===
1. Fintech - Modal Rp 100 juta
2. Insurance - Modal Rp 500 juta  
3. Investment - Modal Rp 1 miliar

=== VALIDASI ===
- Company name minimal 2 karakter
- playerData harus ada (jika tidak ada, redirect ke character creation)

=== FLOW ===
1. useEffect: Load temp_player dari localStorage
2. Jika tidak ada → redirect ke /onboarding/character
3. User input company name
4. User pilih business type
5. Submit → initializeGame()
6. Delete temp_player
7. Redirect ke /game

=== DATA CLEANUP ===
Kenapa hapus temp_player?
- Data sudah disimpan ke gameStore (business-simulator-save)
- temp_player hanya untuk transit antar onboarding pages
- Hindari duplikasi data dan confusion

=== GAMESTOTE INTEGRATION ===
initializeGame(playerName, playerAge, companyName, businessType)
- Create Player object dengan id, name, age, exp, level, createdAt
- Create Company object dengan id, name, type, capital, balance, dll
- Initialize business state (fintech/insurance/investment)
- Save semua ke store dan localStorage

=== UI COMPONENTS ===
1. Header dengan greeting (nama player)
2. Input company name
3. Grid 3 buttons untuk business type selection
4. Summary card (ringkasan sebelum submit)
5. Submit button

=== RESPONSIVE ===
- Desktop: 3 kolom business options
- Mobile: 1 kolom (stacked)
- Max width 2xl untuk readability

=== ERROR HANDLING ===
Error: "Cannot read property 'name' of null"
  → playerData masih null
  → Tambah loading state: if (!playerData) return <Loading>

Error: "initializeGame is not a function"
  → Check import gameStore
  → Check gameStore.ts exports initializeGame

Error: "Redirect ke character creation terus loop"
  → Check localStorage ada temp_player atau tidak
  → Clear localStorage dan coba lagi

=== NEXT STEPS ===
Buat game dashboard yang:
- Load data dari gameStore
- Tampilkan stats (balance, revenue, profit, dll)
- Render business-specific UI (fintech/insurance/investment)
```

---

## **✅ Summary**

**File yang sudah dibuat:**

1. ✅ `app/onboarding/character/page.tsx` - Character creation
2. ✅ `app/onboarding/company/page.tsx` - Company creation

**Flow lengkap onboarding:**
```
Landing Page (belum dibuat)
    ↓
Character Creation (/onboarding/character)
    ↓ (save temp_player)
Company Creation (/onboarding/company)
    ↓ (initializeGame, delete temp_player)
Game Dashboard (/game) ← NEXT STEP